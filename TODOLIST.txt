Opacity on button backgrounds - Might need to change to SASS variables if possible? if not might need to change everything to rgba isntead of hex?

Fix bug where page crashes when refresh job site as object is empty whilst waiting for fetch call.

Check if company url is there, and is more than just 'http'. if not then put some default text.